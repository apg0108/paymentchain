spring:
    cloud:
        config:
            discovery:
                enable: true
                service-id: config-server
            failFast: true
            name: config-client
            profile: development
            username: admin
            password: qwerty
            retry:
                max-attempts: 1
                max-interval: 1000